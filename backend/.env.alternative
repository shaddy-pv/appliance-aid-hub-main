# Alternative MongoDB Atlas Configuration (if SSL issues persist)

# Option 1: With SSL parameters
MONGODB_URI=mongodb+srv://shadanmd566_db_user:jNAbZPESIvS0hneP@cluster1.8qe9rwi.mongodb.net/home-services?retryWrites=true&w=majority&tls=true&tlsAllowInvalidCertificates=true

# Option 2: Standard format (try this if Option 1 fails)
# MONGODB_URI=mongodb+srv://shadanmd566_db_user:jNAbZPESIvS0hneP@cluster1.8qe9rwi.mongodb.net/?retryWrites=true&w=majority

# Option 3: With explicit database
# MONGODB_URI=mongodb+srv://shadanmd566_db_user:jNAbZPESIvS0hneP@cluster1.8qe9rwi.mongodb.net/home-services

# Option 4: Local MongoDB (for testing)
# MONGODB_URI=mongodb://localhost:27017/home-services

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-2024
ACCESS_TOKEN_TTL_MINUTES=15
REFRESH_TOKEN_TTL_DAYS=14

# Server Configuration
PORT=4000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:8080,http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
